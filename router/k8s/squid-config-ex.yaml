apiVersion: v1
kind: ConfigMap
metadata:
  name: squid-config
  namespace: watcher
data:
  squid.conf: |
    # 안전한 기본값. bootstrap-prerequisites.sh가 환경별 CIDR로 교체한다.
    http_port 3000
    http_access deny all
    cache deny all
    cache_dir null /dev/null
    cache_mem 0 MB
